import enGB from './en-gb'
import pl from './pl'

export default {
  'en-gb': enGB, // Browsers can provide locale in either format
  'en': enGB,
  'pl': pl, // Browsers can provide locale in either format
  'pl-pl': pl
}
